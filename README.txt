READ ME:
OPEN A TERMINAL IN THIS FOLDER AND RUN:
python3 -m pip install -e .

EXAMPLE WINDOW:

# EXAMPLE WINDOW!!

import window.window

window.window.create()
window.window.set_title("Hello, world!")
window.window.size(500, 600)
